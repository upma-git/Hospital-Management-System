@model Hospital_Management_System.Models.Account

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5 col-lg-4">
            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-body p-4">
                    <h3 class="text-center mb-4 text-primary">🔐 Login</h3>

                    @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-group mb-3">
                            <label class="form-label fw-semibold">👤 Username</label>
                            @Html.TextBoxFor(m => m.username, new { @class = "form-control rounded-3", placeholder = "Enter your username" })
                        </div>

                        <div class="form-group mb-4">
                            <label class="form-label fw-semibold">🔒 Password</label>
                            @Html.PasswordFor(m => m.password, new { @class = "form-control rounded-3", placeholder = "Enter your password" })
                        </div>

                        <button type="submit" class="btn btn-primary w-100 rounded-3 fw-bold">Login</button>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
